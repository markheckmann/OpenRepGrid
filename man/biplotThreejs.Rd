% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biplot-threejs.r
\name{biplotThreejs}
\alias{biplotThreejs}
\title{Interactive 3D biplot using three.js}
\usage{
biplotThreejs(
  x,
  dim = 1:3,
  center = 1,
  normalize = 0,
  g = 0,
  h = 1 - g,
  col.active = NA,
  col.passive = NA,
  unity = TRUE,
  scale.e = 0.9,
  file = NULL,
  launch = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{repgrid} object.}

\item{dim}{Dimensions (principal components) to display (default \code{1:3}).}

\item{center}{Centering of the grid data. \code{0} = no centering, \code{1} = row mean
(default), \code{2} = column mean, \code{3} = double centering, \code{4} = midpoint
centering.}

\item{normalize}{Normalization. \code{0} = none (default), \code{1} = normalize rows,
\code{2} = normalize columns.}

\item{g}{Power of the singular values assigned to the construct coordinates
(default \code{0}).}

\item{h}{Power of the singular values assigned to the element coordinates
(default \code{1 - g}).}

\item{col.active}{Columns (elements) actively used in the SVD. By default
all elements are active.}

\item{col.passive}{Supplementary columns projected into the biplot.}

\item{unity}{Logical. Scale coordinates so that the longest element and
construct vectors have unit length (default \code{TRUE}).}

\item{scale.e}{Scaling factor for element coordinates (default \code{0.9}).}

\item{file}{Path for the output HTML file. If \code{NULL} (default), a temporary
file is created.}

\item{launch}{Logical. Open the HTML file in the default browser (default
\code{TRUE}).}

\item{...}{Additional arguments passed to \code{\link[=calcBiplotCoords]{calcBiplotCoords()}}.}
}
\value{
The file path of the generated HTML file (invisibly).
}
\description{
Opens an interactive 3D biplot in the browser using three.js. The
visualization supports orbit/zoom/pan controls, toggling of elements,
constructs, axes, and projection lines, as well as hover tooltips showing
representation quality.
}
\examples{
\dontrun{
biplotThreejs(boeker)
biplotThreejs(boeker, dim = c(1, 2, 3), g = 1, h = 1)
}
}
\seealso{
\code{\link[=biplot2d]{biplot2d()}}, \code{\link[=biplot3d]{biplot3d()}}, \code{\link[=biplotSlaterThreejs]{biplotSlaterThreejs()}}, \code{\link[=biplotEsaThreejs]{biplotEsaThreejs()}}
}
